services:
  payments:
    build: .
    container_name: agent-souq-payments
    restart: unless-stopped
    env_file: .env
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
      - ./public:/app/public
